B = A % 8
B = B ^ 3 = (A % 8) ^ 3
C = A // 2**(B) = A // 2**((A % 8) ^ 3)
B = B ^ C = ((A % 8) ^ 3) ^ (A // 2**((A % 8) ^ 3))
B = B ^ 3 = (((A % 8) ^ 3) ^ (A // 2**((A % 8) ^ 3))) ^ 3
A = A // 2**3
-> B % 8
jne

B % 8 = 0, A = 0


